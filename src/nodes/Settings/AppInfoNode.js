import AbstractHippoNode from "../AbstractHippoNode";

export default class AppInfoNode extends AbstractHippoNode{
  constructor(appJson, path) {
    super(appJson, path);
  }

  process(appJson, path, nodeJson) {
  }

}
